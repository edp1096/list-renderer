{
  "name": "hello-esbuild",
  "version": "1.0.0",
  "license": "WTFPL",
  "type": "module",
  "scripts": {
    "prestart": "tsc-watch",
    "dry": "yarn build | node dist/main.js",
    "build": "node esbuild.build.js",
    "watch": "node esbuild.watch.js"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "tslint": "^6.1.3"
  },
  "dependencies": {
    "moment": "^2.29.3"
  }
}
